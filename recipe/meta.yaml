{% set version = "1.0.9" %}
{% set posix = 'm2-' if win else '' %}

package:
  name: r-varnish
  version: {{ version|replace("-", "_") }}

source:
  url: https://github.com/carpentries/varnish/archive/refs/tags/{{ version }}.tar.gz
  sha256: 9de35a4472176f87f01589a6233ee69d59c19e00069ba5d3d486feac1fe0980b

build:
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/
  missing_dso_whitelist:
    - '*/R.dll'        # [win]
    - '*/Rblas.dll'    # [win]
    - '*/Rlapack.dll'  # [win]

requirements:
  build:
    - {{ posix }}filesystem        # [win]
    - {{ posix }}git
    - {{ posix }}zip               # [win]
    - cross-r-base {{ r_base }}    # [build_platform != target_platform]
  host:
    - r-base
  run:
    - r-base

test:
  commands:
    - $R -e "library('varnish')"           # [not win]
    - "\"%R%\" -e \"library('varnish')\""  # [win]

about:
  home: https://github.com/carpentries/varnish
  license: MIT
  summary: The Carpentries Lesson HTML Templates
  description: |
    This project does not contain any useful R code.
    It contains HTML templates with mustache syntax, CSS, and JavaScript.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - conda-forge/r
    - matthewfeickert
